# Phase 2 notes (single-tenant)

This project is configured as **single-tenant**.

- No organization partitioning field exists in the schema.
- Reads are governed by visibility rules (hierarchy/state/self), not organization boundaries.

